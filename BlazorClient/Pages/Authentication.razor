@page "/authentication/{action}"

<RemoteAuthenticatorView Action="@Action">
    @* These are needed to avoid default messages, can also be customized *@
    <LoggingIn />
    <LogInFailed />
    <LogOut />
    <LogOutSucceeded>
        <RedirectToHome />
    </LogOutSucceeded>
</RemoteAuthenticatorView>

@code {
    [Parameter] public string Action { get; set; }
}
